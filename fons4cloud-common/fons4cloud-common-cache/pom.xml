﻿<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>fons4cloud-common</artifactId>
        <groupId>com.fons.cloud</groupId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>fons4cloud-common-cache</artifactId>
    <modelVersion>4.0.0</modelVersion>
    <description>通用的缓存模块</description>
    <packaging>jar</packaging>

    <properties>
        <redisson.version>3.48.0</redisson.version>
        <jetcache.version>2.7.5</jetcache.version>
        <caffeine.cache.version>3.1.8</caffeine.cache.version>
    </properties>

    <dependencies>
        <!-- redisson -->
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson-spring-boot-starter</artifactId>
            <version>${redisson.version}</version>
        </dependency>

        <!-- fons4cloud-common-util -->
        <dependency>
            <groupId>com.fons.cloud</groupId>
            <artifactId>fons4cloud-common-util</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- fons4cloud-stream -->
        <dependency>
            <groupId>com.fons.cloud</groupId>
            <artifactId>fons4cloud-common-stream</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- springboot cache -->
        <!--<dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>-->

        <!-- alibaba-jetcache -->
        <dependency>
            <groupId>com.alicp.jetcache</groupId>
            <artifactId>jetcache-starter-redisson</artifactId>
            <version>${jetcache.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- caffeine -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>${caffeine.cache.version}</version>
        </dependency>
    </dependencies>

</project>