# 框架控制面实施报告

> 功能标识：`admin-control-plane`
> 任务范围：`T001-T026（T022 未关闭）`
> 实现确认依据：用户最新消息“继续执行”
> SDD 等级：`S2`
> 更新日期：2026-07-09

## 0. 最新执行增量（2026-07-09 CR-001 gateway 认证优化与 admin 双层权限）

- 本次授权依据：用户最新消息“继续执行”。
- 本次完成任务：T023、T024、T025、T026。
- 本次未关闭任务：T022。原因是代码级和自动化契约回归已通过，但真实 gateway/admin/auth 重启后的 Bearer token 转发复检尚未执行；真实 Nacos/Redis 写入、发布、漂移验证和清理仍按用户确认暂缓至前后端集成测试。
- 本次核心变更：
  - `AuthorizationManager` 将业务免 Token 判断前移到运行期匿名放行判断；受保护请求不再写死 `/admin/**` 特例，统一委派 `AuthPermissionService` 复用框架授权资源判断。
  - `ResourceServerConfiguration` 不再把业务免 Token URI 固化进启动期 `pathMatchers`，启动期白名单仅保留静态端点。
  - `AuthPermissionService` 新增 `isPermitAnonymousRequest`，`AbstractAuthPermissionService` 将匿名白名单判断与完整权限资源判断拆开，避免 gateway 每个受保护请求都触发“无 authorities”的完整权限判断告警；业务免 Token URI 读取增加 5 秒只读缓存。
  - `AdminAuthController` 的登录和刷新入口补充 `@BsWebAdvice(requiredToken = false)`，用于通过框架切面注册业务免 Token URI；退出接口仍保持受 Token 保护。
  - 新增 `AdminPermission` 作为 admin-service 内部 RBAC 权限声明，`AdminSecurityInterceptor` 改为读取 `@AdminPermission`；admin REST Controller 使用 `@AuthenticationResource(authorities = "ADMIN")` 表达 gateway 入口准入，使用 `@AdminPermission` 表达内部 RBAC，避免细粒度 admin 权限被误注册为 gateway 全局资源。
- RED 记录：
  - `AuthorizationManagerTest` 初始失败：运行期业务免 Token URI 未在无认证场景放行；受保护请求仍存在 gateway 本地 `/admin/**` 特例判断，未完全复用框架授权资源。
  - `AdminSecurityInterceptorTest,AdminResourceAnnotationBoundaryTest` 初始编译失败：`AdminPermission` 尚不存在，说明 admin 内部 RBAC 与全局资源注解尚未拆分。
- GREEN 记录：
  - `mvn -pl fons4cloud-gateway -am -Dtest=AuthorizationManagerTest "-Dsurefire.failIfNoSpecifiedTests=false" test` 通过，`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`。
  - `mvn -pl fons4cloud-auth/fons4cloud-auth-core -Dtest=AuthorizationResourceRepositoryTest test` 通过，`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`。
  - `mvn -pl fons4cloud-admin/fons4cloud-admin-service "-Dtest=AdminResourceAnnotationBoundaryTest,AdminSecurityInterceptorTest" test` 通过，`Tests run: 6, Failures: 0, Errors: 0, Skipped: 0`。
  - 前序同轮已通过 `mvn -pl fons4cloud-admin/fons4cloud-admin-service test`，`Tests run: 79, Failures: 0, Errors: 0, Skipped: 0`。
- 边界扫描：
  - `rg "@AuthenticationResource\\(authorities = \"ADMIN\"\\)" fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/interfaces/rest` 可命中受保护 REST 接口；`@AdminPermission` 仍用于内部 RBAC，且不使用 `ADMIN` 作为内部功能权限。
- S2 门禁结论：T023-T026 已具备代码级交付证据；T022 继续保持未关闭，发布建议仍为暂不发布。

## 0.1 历史执行增量（2026-07-08 T022 gateway 路由复检）

- 本次授权依据：用户最新消息确认已在 Nacos 补充 admin-service gateway 路由，并确认真实 Nacos/Redis 写入、发布、漂移和清理后续通过前后端集成测试验证。
- 本次复检结论：T022 仍未完全关闭，暂不发布。
- 已新增关闭证据：
  - 已关闭旧 8002 进程，带服务端客户端密钥重新启动 `admin-service.jar`；当前 admin-service 监听 `0.0.0.0:8002`，PID `9180`，启动参数包含 `-Xmx512m`。
  - 本地健康端点 `http://127.0.0.1:8002/actuator/health` 返回 HTTP 200。
  - Nacos MCP 复核 `public/DEFAULT_GROUP/admin-service` 返回实例 `192.168.1.50:8002`，`healthy=true`、`enabled=true`、`ephemeral=true`。
  - Nacos MCP 复核 `public/DEFAULT_GROUP/gateway-service` 返回实例 `192.168.1.50:9527`，`healthy=true`、`enabled=true`、`ephemeral=true`。
  - Nacos `gateway-routing.json` 当前 md5 为 `619429171e76d67b1c9548e7dd7be9eb`，已包含 `id=admin-service`、`uri=lb://admin-service`、`Path=/admin/**`。
  - MySQL 只读复核显示初始化 DML 已按实际 `sys-admin` clientId 落库：`sys_auth.oauth_client` 有效数据 1 条，`sys_auth.account` 有效数据 1 条，`sys_admin.admin_user` 有效数据 1 条，`ADMIN_ROOT` 角色绑定有效。
  - `sys_auth.oauth_client` 中 `client_id=sys-admin` 的授权类型为 `password,sms,refresh_token`，状态启用；`sys_auth.account` 中 `client_id=sys-admin`、`username=admin_root` 的认证账号状态为 `ACTIVE`。报告不记录 clientSecret、密码密文或任何明文凭据。
  - 新增 `AdminAuthApplicationServiceTest` RPC 异常兜底用例，覆盖登录、刷新、退出三类 RPC 抛异常场景；RED 阶段 3 个用例失败，修复后 `AdminAuthApplicationServiceTest` 8 个用例全部通过。
  - 按当前 `AdminLoginRequest` 契约直连调用 `http://127.0.0.1:8002/admin/auth/login` 返回 HTTP 200、业务码 `000000`，响应中存在 accessToken；报告不记录 token 明文。
  - 前序复检已验证直连 `/admin/auth/refresh-token` 与 `/admin/auth/logout` 返回业务成功，且退出返回 `true`。
  - 前序复检已验证直连 admin-service 时按 gateway 注入规则模拟 `auth_user` 头，`/admin/services` 和 `/admin/observability/actuator/probe` 均返回业务成功。
- 新增阻断证据：
  - 经 gateway 调用 `POST http://127.0.0.1:9527/admin/auth/login` 返回 HTTP 401、业务码 `400001`、消息 `令牌不存在或已过期`。这说明路由已不是当前卡点，登录入口仍被 gateway token 校验拦截，`GLOBAL_IGNORED_ACCESS_TOKEN_API` 当前未发现对应 key。
  - 使用直连 admin-service 登录获得的 Bearer token 调用 `GET http://127.0.0.1:9527/admin/services` 返回 HTTP 403、业务码 `400002`、消息 `没有权限访问`。请求在 gateway 全局资源权限校验阶段被拒绝，尚未转发到 admin-service RBAC。
- 暂缓项：
  - 用户已确认真实 Nacos/Redis 写入、发布、漂移验证和清理后续通过前后端集成测试验证，本轮不再直接写共享 Nacos/Redis。
- 下一步关闭前置条件：
  - 明确 gateway 对 `/admin/auth/login`、`/admin/auth/refresh-token`、`/admin/auth/logout` 的免 token 策略，或通过授权资源治理把这些入口发布到忽略 token URI。
  - 明确 gateway 全局权限资源与 admin 自有 RBAC 的边界：要么为 `/admin/**` 对齐 token authorities 与 `services:view` 等权限点，要么将 admin 权限只交给 admin-service 自身 RBAC 处理，避免 gateway 先行拒绝。

## 0.0.1 最新执行增量（2026-07-08 sys-admin 标准对齐）

- 本次授权依据：用户最新消息“按照落库的sys-admin作为标准”。
- 本次变更：
  - admin-service 默认 `admin.auth.client-id` 从 `ADMIN` 调整为 `sys-admin`，与当前 `sys_auth.oauth_client` 和 `sys_auth.account` 落库事实一致。
  - 聚焦测试中的控制面客户端样例同步为 `sys-admin`；`ADMIN_ROOT` 角色编码、认证服务账号 `role=ADMIN`、错误码 `ADMIN_*` 不属于 OAuth Client ID，本次不调整。
  - 初始化 DML 草案、DDL 注释、技术设计说明书和任务规划的当前标准口径同步为 `sys-admin`。
- 当前门禁结论：代码与文档口径已完成标准对齐，但运行中 admin-service 仍是旧进程，必须重新构建并重启后才能重新验证真实登录和 Token 链路。
- 本次验证限制：
  - 聚焦 Maven 测试因沙箱无法访问远程仓库失败；申请外部网络权限时被当前 Codex 使用额度限制拦截，未获得新的测试通过证据。
  - 尝试用环境变量覆盖 `admin.auth.client-id=sys-admin` 另起验证实例时，同样被当前 Codex 使用额度限制拦截，命令未执行。
  - 因此 T022 仍保持未勾选，下一轮需要在可执行命令环境下完成构建、重启和正向 Token 联调。

## 0.1 历史执行增量（2026-07-07 T022 运行闭环复检）

- 本次授权依据：用户提供 admin-service 运行所需 Nacos 环境变量，并要求按该配置继续验证。
- 本次使用环境变量：`NACOS_NAMESPACE=public`、`NACOS_DUBBO_NAMESPACE=public`、`NACOS_SERVER=115.190.114.117:8848`、`NACOS_USERNAME=nacos`、`NACOS_GROUP=DEFAULT_GROUP`；密码使用用户提供值，报告不展开。
- 本次修复：
  - `GatewayRouteGovernanceAdapter` 与 `ActuatorReadAdapter` 显式标注生产构造器 `@Autowired`，避免存在测试构造器时 Spring 运行期误判为缺少默认构造器。
  - `AuthorizationResourceRepository` 兼容 Redis 中历史权限资源值为 `String`、JSON 数组字符串、集合 `toString()` 字符串或集合对象的情况，避免启动扫描重复注册权限资源时报类型转换或 JSON 解析异常。
- 本次新增测试证据：
  - `GatewayRouteGovernanceAdapterTest,ActuatorReadAdapterTest` 聚焦测试通过：`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`。
  - `AuthorizationResourceRepositoryTest` 聚焦测试通过：`Tests run: 3, Failures: 0, Errors: 0, Skipped: 0`。
- 本次真实启动证据：
  - `mvn -pl fons4cloud-admin/fons4cloud-admin-service -am -DskipTests package` 通过，`target/admin-service.jar` 完成 Spring Boot repackage。
  - 使用上述 Nacos 环境变量执行 `java -Xmx512m -jar target/admin-service.jar`，admin-service 监听 `0.0.0.0:8002`，PID `35684`。
  - 本地健康端点 `http://127.0.0.1:8002/actuator/health` 返回 `{"status":"UP"}`。
  - Nacos MCP `public/DEFAULT_GROUP/admin-service` 返回实例 `192.168.1.50:8002`，`healthy=true`、`enabled=true`、`ephemeral=true`。
  - 启动日志 `admin-service-nacos-start-5.out.log` 包含 `Tomcat started on port 8002` 与 `Started AdminMain`；未再出现 `No default constructor`、`Failed to process authentication resource`、`ClassCastException` 或 `JSONException`。
- 本次关闭验证补充：
  - MySQL 只读验证显示 `sys_admin.admin_user` 当前有效数据为 0、`sys_admin.admin_role` 当前有效数据为 0、`sys_admin.admin_permission` 当前有效数据为 23、`sys_auth.oauth_client` 中 `client_id=ADMIN` 当前有效数据为 0。
  - 调用 `POST /admin/auth/login` 使用非真实密钥探测，接口返回 HTTP 500，响应为 `{"success":false,"code":"999999","message":"系统内部错误","data":null}`；启动日志显示 admin-service 已通过 Dubbo 触发 `AccountAuthenticationFacadeService.authenticate`，auth-service 返回业务异常 `租户不存在`。
  - 调用 `POST /admin/observability/actuator/probe` 不带认证上下文返回 HTTP 401；由于当前无 admin 用户绑定和可用 admin token，无法通过受保护 admin API 发起正向 Actuator 探测。
- 本次 DML 补充：
  - 新增 `spec/features/20260630/dml-changes/INIT-admin-auth-root-access.sql`，用于人工审核后初始化 `sys-admin` OAuth Client、sys-admin 客户端下的 ROOT 认证账号、admin 权限点目录、`ADMIN_ROOT` 角色、角色权限关系和首个 ROOT 管理员绑定。
  - DML 中 `clientSecret` 和登录密码均为 BCrypt 密文占位变量，不包含真实明文；脚本不会自动执行，需要用户/DBA 按目标环境替换变量、备份后手动执行。
- 当前门禁结论：T022 运行闭环中的 admin-service 启动、健康检查、Nacos 注册、权限资源扫描异常已关闭；仍不建议发布到共享或生产环境。
- 仍未关闭风险：
  - admin-service 到 auth-service 的 Dubbo 通道已触发，但当前环境缺少 `ADMIN` OAuth Client、admin 用户和角色绑定，无法完成正向 Token 获取、刷新和吊销链路。
  - 未在隔离 Nacos/Redis 环境执行真实路由发布、漂移验证、Redis 黑白名单发布和清理；当前仍避免写共享运行配置。
  - 未通过携带真实 admin token 的 admin API 发起 Actuator 探测链路验证；仅证明 admin-service 自身健康、注册成功，以及未登录访问会被拒绝。

## 0.1 历史执行增量（2026-07-07 T022 闭环复检）

- 本次授权依据：用户最新消息“重新验证是否T022”，指定复检 T022。
- 本次复检任务：T022 关闭 S2 风险门禁和发布就绪检查。
- 当前完成任务：T001-T021。
- 当前未完成任务：T022。
- 门禁结论：未完全关闭，暂不发布。
- 本次新增验证证据：
  - admin-service 自动化回归通过，命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dsurefire.failIfNoSpecifiedTests=false' test`。
  - admin-service 自动化回归结果：`Tests run: 73, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`，完成时间 `2026-07-07T11:44:53+08:00`。
  - MySQL 只读验证重试成功，`sys_admin` 中 10 张 admin 目标表仍存在，查询耗时约 41.26 ms。
  - Nacos 认证和注册中心读取已恢复：`public`、`seata` namespace 可读取，`auth-service` 实例 `192.168.1.50:8001` 健康启用，`gateway-service` 实例 `192.168.1.50:9527` 健康启用。
  - Nacos 网关路由配置可读取：`DEFAULT_GROUP/gateway-routing.json` 返回 `code=0`，包含 `sys-auth -> lb://auth-service`。
  - 真实服务 Actuator 健康端点可达：`http://192.168.1.50:8001/actuator/health` 与 `http://192.168.1.50:9527/actuator/health` 均返回 `{"status":"UP"}`。
  - Redis MCP 可执行 key 查询，`GLOBAL_AUTHORIZATION_RESOURCE*` 与 `*WHITE*` 当前未命中 key，说明连接可用但仍未证明运行期治理数据。
  - 已补充 admin-service 启动类 `com.fons.cloud.admin.AdminMain`，包含 `@EnableDubbo`、`@SpringBootApplication` 和 `@EnableDiscoveryClient`，与 auth-service 启动类风格保持一致。
- 本次仍未关闭风险：
  - admin-service 真实运行联调未验证：启动类和 Spring Boot repackage 已补齐，`target/admin-service.jar` 已生成可执行 Boot Jar；但 `java -jar target/admin-service.jar` 启动时，因 `application.yml` 指定 `logging.config=classpath:logback.xml` 且运行 classpath 中不存在 `logback.xml`，日志系统初始化失败并退出，无法验证服务注册、`/admin/**` REST API、ROOT 初始化、admin 权限链路和 admin 到 auth-service 的真实 Dubbo RPC 调用。
  - 真实 auth-service Dubbo RPC 未验证：虽已取得 auth-service 注册实例，但仍未通过 admin-service 实际调用 `AccountAuthenticationFacadeService`、OAuth Client 管理 RPC。
  - 真实 Nacos/Redis 写入未执行：仍未在隔离环境执行路由发布、漂移验证、Redis 黑白名单发布和清理。
- 本次启动与注册复检：
  - `mvn -pl fons4cloud-admin/fons4cloud-admin-service -am -DskipTests package` 通过，Spring Boot repackage 成功，`target/admin-service.jar` 已生成可执行 Boot Jar。
  - `java -jar target/admin-service.jar` 启动失败，stderr 明确报错：`class path resource [logback.xml] cannot be resolved to URL because it does not exist`。
  - 启动 35 秒后进程已退出，`http://127.0.0.1:8002/actuator/health` 不可达，因此未产生 admin-service 真实注册实例、REST API、ROOT 初始化或 Dubbo RPC 联调证据。
  - `application.yml` 还导入 `classpath:nacos_config.yml`，当前 admin-service resources 下也未发现该文件；该项尚未进入实际失败阶段，但属于修复 `logback.xml` 后的下一项启动配置风险。
- 发布就绪判断：否。T022 继续保持未勾选；Nacos 服务发现、外部 Actuator 可达性、admin-service 启动类和可执行 Boot Jar 打包已补证，但仍需补齐 admin-service 运行配置、真实启动注册、真实 admin 到 auth RPC 联调、隔离 Nacos/Redis 写入验证，或获得用户对未验证项的明确暂缓确认。

## 0.1 历史执行增量（2026-07-03 T022 S2 风险门禁）

- 本次授权依据：用户最新消息“继续推进admin任务”，未指定任务 ID，按任务规划继续执行全部未完成任务。
- 本次执行任务：T022 关闭 S2 风险门禁和发布就绪检查。
- 当前完成任务：T001-T021。
- 当前未完成任务：T022。
- 本次产物：新增 `spec/features/20260630/checklists/框架控制面-S2风险检查清单.md`。
- 门禁结论：未完全关闭，暂不发布。
- 已关闭风险：DDL 执行确认、SQL 当前结构快照、admin RBAC 鉴权、Nacos 路由配置读取、回滚快照、敏感信息脱敏、admin-service 全量自动化回归。
- 未关闭风险：
  - 服务发现实例查询未验证：Nacos MCP `list_service_instances` 对 `fons4cloud-auth`、`auth-service`、`gateway-service` 返回 404。
  - 真实 auth-service Dubbo RPC 未验证：当前仅有 Mock/契约测试覆盖登录、刷新、吊销 Token 与 OAuth Client 管理。
  - 真实 Actuator 探测未验证：缺少可访问的服务实例地址。
  - 真实 Nacos/Redis 写入未执行：为避免修改共享运行配置，本次没有直接发布网关路由或 Redis 黑白名单。
- 发布就绪判断：否。T022 不勾选完成，需补齐隔离联调环境验证或获得用户对未验证项的明确暂缓确认。

## 0.2 历史执行增量（T021 集成回归验证）

- 本次授权依据：用户最新消息“继续推进admin任务”，未指定任务 ID，按任务规划继续执行全部未完成任务。
- 本次完成任务：T021 执行跨服务集成和回归验证。
- 当前未完成任务：T022。
- 自动化回归命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 自动化回归结果：通过，`Tests run: 73, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`，完成时间 `2026-07-03T12:07:26+08:00`。
- 外部环境只读验证：
  - MySQL：`sys_admin` 中 10 张 admin 目标表存在；首次查询出现 `ECONNRESET`，重试成功，查询耗时约 52.71 ms。
  - Nacos：`public` 和 `seata` namespace 可列出；`DEFAULT_GROUP/gateway-routing.json` 可读取，配置类型为 JSON，包含网关动态路由内容。
  - Redis：`GLOBAL_AUTHORIZATION_RESOURCE*` 与 `*WHITE*` 查询可执行，当前未命中 key，说明连接与认证可用但未证明运行期已有对应数据。
- 未写入项：未直接向生产/共享 Nacos 发布路由，未直接写 Redis 黑白名单，未写真实 `sys_admin` CRUD 测试数据。
- 未验证项：
  - Nacos 服务实例查询：`fons4cloud-auth`、`auth-service`、`gateway-service` 通过当前 MCP `list_service_instances` 均返回 404，无法证明服务发现实例列表。
  - auth-service 真实 Dubbo RPC：未启动或绑定真实服务联调，本次以 `AdminAuthApplicationServiceTest`、`OAuthClientGovernanceAdapterTest` 等 Mock/契约测试覆盖。
  - 真实 Actuator 探测：未获取可访问的服务实例地址，本次以 `ActuatorReadAdapterTest` 覆盖白名单、不可用目标和超时边界。
- 当前结论：T021 作为“回归验证与外部环境能力确认”完成；真实写入/服务实例/Actuator 联调风险进入 T022 风险门禁记录。
- 后续事项：继续 T022，生成并关闭 S2 风险检查清单。

## 0.3 历史执行增量（T020 数据安全验证）

- 本次授权依据：用户最新消息“继续推进admin任务”，未指定任务 ID，按任务规划继续执行全部未完成任务。
- 本次完成任务：T020 验证数据安全、脱敏和敏感字段处理。
- 当前未完成任务：T021-T022。
- RED 记录：新增 `GovernanceSecurityDataHandlingTest` 后，首次运行 `GovernanceSecurityDataHandlingTest` 失败；失败原因是 `GovernanceSnapshot.contentSummary` 直接由完整配置正文脱敏得到，虽然隐藏了 `clientSecret/token`，但仍暴露 `10.0.0.1`、`admin-route`、`lb://admin-service` 等 IP 列表和路由配置正文。
- GREEN 修复：`GovernanceSnapshot.toEntity()` 改为生成保守摘要，仅保存“配置内容已脱敏，长度=...，hash=...”；完整快照正文仍保留在 `content` 字段，由后续详情权限控制，不再进入列表/审计摘要。
- 本次变更文件：`GovernanceSnapshot`、`GovernanceSecurityDataHandlingTest`。
- 日志检查：扫描 admin-service 主代码未发现 `log.`、`LOGGER`、`Slf4j`、`System.out` 或 `printStackTrace` 显式输出敏感字段；当前 admin-service 资源目录无独立 `logback*.xml`。
- 验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=GovernanceSecurityDataHandlingTest,GovernanceChangeTest,ChangeApplicationServiceTest,GovernancePublishServiceTest,OAuthClientGovernanceAdapterTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 验证结果：通过，`Tests run: 19, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`，完成时间 `2026-07-03T12:04:08+08:00`。
- AC 覆盖增量：AC-005、AC-007、AC-008 增加审计摘要、发布失败错误摘要、快照摘要和 OAuth Client 密钥治理的敏感字段处理证据。
- 剩余风险：完整配置正文仍按设计保存在 `admin_governance_change.content` 与 `admin_governance_snapshot.content`，必须依赖 T010/T019 已落地的权限控制和后续 T022 风险检查确认详情接口访问边界。
- 后续事项：继续 T021，执行跨服务集成和回归验证。

## 0.4 历史执行增量（T012-T019 验证闭环）

- 本次授权依据：用户最新消息“继续推进admin任务”，未指定任务 ID，按任务规划继续执行全部未完成任务。
- 本次完成任务：T012 统一变更治理状态机和快照服务、T013 治理目标适配器端口和变更 API 编排、T014 网关路由 Nacos 治理适配器、T015 限流黑白名单治理适配器、T016 授权资源治理适配器、T017 OAuth Client 治理适配器、T018 服务发现查询和 Actuator 只读探测、T019 admin REST API 控制器汇总。
- 当前未完成任务：T020-T022。
- 本次修复内容：分组治理 REST API 新增 `GroupedGovernanceDraftCreateRequest`，由路径固定 `domain/resourceType`，避免调用方为 `/admin/gateway/routes`、`/admin/traffic`、`/admin/access`、`/admin/clients` 再传冗余治理域和资源类型；新增 `GovernanceDraftRequestConverter` 复用 MapStruct 做请求转换。
- 本次质量修复：授权资源治理显式限定标准 HTTP 方法，避免 `BAD_/path` 这类非标准资源进入权限配置；Nacos 路由适配器测试移除未使用的通用 stub，保持 Mockito 严格模式。
- 本次变更文件：`GroupedGovernanceDraftCreateRequest`、`GovernanceDraftRequestConverter`、`GatewayGovernanceController`、`TrafficGovernanceController`、`AccessGovernanceController`、`ClientGovernanceController`、`AccessResourceGovernanceAdapter`、`GatewayRouteGovernanceAdapterTest`、`GovernanceGroupControllerTest`。
- 验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=GovernanceChangeTest,ChangeApplicationServiceTest,GovernancePublishServiceTest,ChangeControllerTest,GatewayRouteGovernanceAdapterTest,TrafficIpGovernanceAdapterTest,AccessResourceGovernanceAdapterTest,OAuthClientGovernanceAdapterTest,ServiceDiscoveryReadAdapterTest,ActuatorReadAdapterTest,GovernanceGroupControllerTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 验证结果：通过，`Tests run: 39, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`，完成时间 `2026-07-03T11:59:29+08:00`。
- AC 覆盖增量：AC-001、AC-002、AC-003、AC-004、AC-005、AC-006、AC-007、AC-008、AC-009 均增加第一版 REST/API/适配器/状态机聚焦测试证据；T020-T022 仍需补充敏感数据验证、跨服务集成回归和 S2 风险门禁结论。
- 剩余风险：当前通过的是 Mock/单元级聚焦测试；真实 Nacos 发布、Redis 黑白名单、auth-service RPC、服务发现和 Actuator 探测的跨服务验证仍归入 T021。
- 后续事项：继续 T020，验证 Token、clientSecret、配置正文、IP 列表和操作人信息的响应、日志、审计、快照安全边界。

## 0.5 历史执行增量（T009-T011）

- 本次授权依据：用户最新消息“直接执行剩余的全量任务”，未指定任务 ID，按任务规划继续执行全部未完成任务。
- 本次完成任务：T009 管理员用户、角色、权限点和 ROOT 初始化。
- 当前未完成任务：T010-T022。
- 本次实现结果：补齐认证账号查询契约中的 `clientId` 和 `username` 查询能力；admin-service 实现管理员绑定、禁用、角色授权、权限点目录初始化和首个 ROOT 初始化。
- 本次变更文件：`AccountQueryParams`、`AccountInfo`、`AccountFacadeServiceImpl`、`AdminResultCode`、`AdminAccessApplicationService`、`AdminUser`、`AdminRole`、`AdminPermissionCatalog`、`AdminAccountClient`、`DubboAdminAccountClient`、`AdminAccessProperties`、`AdminAccessInitializer`、`AdminAccessApplicationServiceTest`、`AdminAccessInitializerTest`。
- TDD 记录：新增 `AdminAccessApplicationServiceTest` 覆盖控制面客户端准入、业务客户端拒绝、禁用绑定、权限点目录初始化和 ROOT 角色初始化；新增 `AdminAccessInitializerTest` 覆盖启动配置指定 ROOT 用户名后的初始化调用。
- 验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=AdminAccessApplicationServiceTest,AdminAccessInitializerTest,AdminAuthApplicationServiceTest,AdminAuthControllerTest,AdminPersistenceMappingTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 验证结果：通过，`Tests run: 18, Failures: 0, Errors: 0, Skipped: 0`。
- AC 覆盖增量：AC-007 增加管理员绑定准入校验、权限点目录初始化和 `ADMIN_ROOT` 初始化证据；绑定时通过 auth RPC 查询账号并校验 `account.clientId == ADMIN`，业务客户端账号返回 `ADMIN_ACCOUNT_CLIENT_MISMATCH`。
- 质量复盘：账号客户端归属校验下沉到 `AdminUser` 领域模型；内置 ROOT 角色由 `AdminRole` 封装；权限点目录由 `AdminPermissionCatalog` 统一定义；禁用 `admin_user` 不修改认证服务账号状态，不保存 `source_client_id`。
- 剩余风险：T009 补齐了 auth RPC 账号查询契约，后续 T021 仍需做跨服务集成回归；真实 `sys_admin` 写库 CRUD 验证仍按规划放入 T021 隔离集成回归。
- 后续事项：继续 T010，实现 admin 鉴权拦截和授权资源注册。

### T010 执行记录

- 本次完成任务：T010 admin 鉴权拦截和授权资源注册。
- 当前未完成任务：T011-T022。
- 本次实现结果：新增 admin 自有 RBAC 授权服务、MVC 鉴权拦截器和 Web 配置；除 `/admin/auth/login`、刷新和退出认证入口外，其它 `/admin/**` 需要 `AUTH_USER` 且必须声明 `@AuthenticationResource` 权限点，否则默认拒绝。
- 本次变更文件：`AdminAuthorizationDecision`、`AdminAuthorizationService`、`AdminSecurityInterceptor`、`AdminWebSecurityConfiguration`、`AdminAuthorizationServiceTest`、`AdminSecurityInterceptorTest`。
- TDD 记录：新增 `AdminAuthorizationServiceTest` 覆盖未绑定、已禁用、缺少权限、具备权限四类 RBAC 判断；新增 `AdminSecurityInterceptorTest` 覆盖未登录、未配置资源默认拒绝、拒绝用户和允许用户。
- 验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=AdminAuthorizationServiceTest,AdminSecurityInterceptorTest,AdminAccessApplicationServiceTest,AdminAccessInitializerTest,AdminAuthApplicationServiceTest,AdminAuthControllerTest,AdminPersistenceMappingTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 验证结果：通过，`Tests run: 26, Failures: 0, Errors: 0, Skipped: 0`。
- AC 覆盖增量：AC-007 增加运行期未登录、未绑定、禁用、缺少权限和具备权限的拒绝/放行证据；AC-008 保持 `/admin/auth/**` 前序 REST 入口回归通过。
- 质量复盘：拦截器复用已有 `AUTH_USER` 传递约定和 `AuthUtils`；权限点复用 `@AuthenticationResource`；未声明权限点的 admin 接口默认拒绝，避免全局资源仓库未配置时默认放行。
- 剩余风险：当前仅有认证 REST 入口已落地，后续 T019 汇总 REST API 时新增的 admin 控制器必须按权限点补齐 `@AuthenticationResource`，否则会被 T010 拦截器拒绝。
- 后续事项：继续 T011，实现审计记录与脱敏摘要能力。

### T011 执行记录

- 本次完成任务：T011 审计记录与脱敏摘要能力。
- 当前未完成任务：T012-T022。
- 本次实现结果：新增审计查询响应 DTO、审计领域模型、审计应用服务和 `/admin/audits` REST 查询入口；审计摘要写入前会对 token、refreshToken、accessToken、clientSecret、password、secret 等字段做脱敏。
- 本次变更文件：`GovernanceAuditResponse`、`GovernanceAudit`、`AuditApplicationService`、`AuditController`、`AuditApplicationServiceTest`、`AuditControllerTest`。
- TDD 记录：新增 `AuditApplicationServiceTest` 覆盖敏感字段脱敏、查询映射和详情查询；新增 `AuditControllerTest` 覆盖审计列表与详情 REST 响应。
- 验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=AuditApplicationServiceTest,AuditControllerTest,AdminAuthorizationServiceTest,AdminSecurityInterceptorTest,AdminAccessApplicationServiceTest,AdminAccessInitializerTest,AdminAuthApplicationServiceTest,AdminAuthControllerTest,AdminPersistenceMappingTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 验证结果：通过，`Tests run: 31, Failures: 0, Errors: 0, Skipped: 0`。
- AC 覆盖增量：AC-005 增加审计结构化写入、脱敏摘要、按条件查询和详情查询证据；AC-007 增加审计查询接口 `audits:view` 授权资源声明。
- 质量复盘：审计字段结构化保存，敏感值在领域模型转换实体时统一脱敏；Controller 只负责参数接收和响应返回；后续草稿、发布、回滚接入同一个 `AuditApplicationService`。
- 剩余风险：当前 T011 已提供通用审计能力和查询入口，草稿、发布、回滚等尚未实现的操作会在后续 T012-T019 对应流程中接入审计写入。
- 后续事项：继续 T012，实现统一变更治理状态机和快照服务。

### T012 执行记录（已完成）

- 本次执行任务：T012 统一变更治理状态机和快照服务。
- 当前实现状态：已新增 `GovernanceChange`、`GovernanceRelease`、`GovernanceSnapshot` 三个领域模型，并新增 `ChangeApplicationService` 编排草稿创建、校验成功、校验失败、发布成功、发布失败、漂移检测、创建回滚变更、回滚成功和回滚失败。
- 本次测试补充：已新增 `GovernanceChangeTest` 和 `ChangeApplicationServiceTest`，覆盖状态迁移、草稿创建、校验结果、发布记录、快照类型、漂移检测、回滚生成新变更以及回滚失败状态。
- 验证结果：T012 已纳入 T012-T019 聚焦 Maven 测试，`Tests run: 39, Failures: 0, Errors: 0, Skipped: 0`，可勾选完成。

### T013-T019 执行记录（已完成）

- 本次授权依据：用户最新消息“继续任务，先不关注这个阻塞”，允许在 T012 Maven 验证阻塞未解除时继续后续编码。
- 当前实现状态：T013-T019 第一版代码与聚焦测试补充已完成，并已通过 L3 Maven 聚焦测试验证；任务规划中 T012-T019 已勾选。
- T013 变更 API 编排：新增 `GovernanceTargetAdapter` 端口、`GovernancePublishService`、`GovernanceChangeResponse`、`GovernanceChangeConverter` 和 `ChangeController`；新增 `GovernancePublishServiceTest`、`ChangeControllerTest`，覆盖测试适配器、校验失败、发布成功、发布失败和不可回滚场景。
- T014 网关 Nacos 适配：新增 `GatewayRouteGovernanceAdapter` 和 `GatewayRouteGovernanceAdapterTest`；读取和发布 `gateway-routing.json`，校验路由 `id`、`uri`、`predicates`，并修正缺失字段被 `String.valueOf(null)` 误判的问题。
- T015 流量治理适配：新增 `TrafficIpGovernanceAdapter` 和 `TrafficIpGovernanceAdapterTest`；复用 `ManualWhiteIpService`、`ManualBlockedIpService`，治理内容为 `whiteIps` 与 `manualBlockedIps`，不新建 Redis 结构，不写行为分析自动黑名单。
- T016 授权资源适配：在 `AuthorizationResourceRepository` 中补充全量读取和整体替换方法；新增 `AccessResourceGovernanceAdapter` 和 `AccessResourceGovernanceAdapterTest`，复用 `GLOBAL_AUTHORIZATION_RESOURCE`、`GLOBAL_IGNORED_ACCESS_TOKEN_API`、`GLOBAL_IDENTIFIER_TOKEN_API`，按 `METHOD_/path` 校验资源。
- T017 OAuth Client 治理适配：新增 `AdminOauthClientManagementClient`、`DubboAdminOauthClientManagementClient`、`OAuthClientGovernanceAdapter` 和测试；admin 仅通过 auth-service RPC 执行新增、修改、启停和密钥轮换，草稿拒绝保存 `clientSecret/newClientSecret` 明文，快照只保存脱敏 `OauthClientInfo`。
- T018 服务发现与 Actuator 只读探测：新增 `ServiceDiscoveryReadAdapter`、`ActuatorReadAdapter` 及测试；服务发现只读展示实例，Actuator 仅允许 `/actuator/health`、`/actuator/info` 等服务端白名单端点，超时 3 秒，不做下线、权重或配置修改。
- T019 REST API 汇总：新增 `ServiceGovernanceController`、`ObservabilityController`、`GatewayGovernanceController`、`TrafficGovernanceController`、`AccessGovernanceController`、`ClientGovernanceController` 和 `GovernanceGroupControllerTest`；分组入口只做参数接收、域/资源类型兜底和应用服务调用，发布、回滚继续走 `/admin/changes` 通用流程。
- 本次新增/修改的关键文件：
  - `fons4cloud-admin/fons4cloud-admin-api/src/main/java/com/fons/cloud/admin/api/response/GovernanceChangeResponse.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/domain/adapter/GovernanceTargetAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/application/GovernancePublishService.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/nacos/GatewayRouteGovernanceAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/limiter/TrafficIpGovernanceAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/auth/AccessResourceGovernanceAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/auth/OAuthClientGovernanceAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/discovery/ServiceDiscoveryReadAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/actuator/ActuatorReadAdapter.java`
  - `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/interfaces/rest/*GovernanceController.java`
  - `fons4cloud-auth/fons4cloud-auth-core/src/main/java/com/fons/cloud/auth/core/AuthorizationResourceRepository.java`
- 验证命令：同本报告“最新执行增量（T012-T019 验证闭环）”。
- 验证结果：通过，`Tests run: 39, Failures: 0, Errors: 0, Skipped: 0`。
- 当前未完成任务：T020-T022。T020-T022 仍需执行数据安全验证、跨服务集成回归和 S2 风险门禁。
- 发布就绪判断：否。当前不是可发布状态，必须完成 Maven 编译/测试、敏感数据验证、集成回归和 S2 检查清单后才能进入交付评审。

## 1. 实施摘要

- 已完成任务：T001-T021。
- 未完成任务：T022。
- 阻塞任务：T022，原因是服务发现实例查询、真实 auth-service Dubbo RPC 和真实 Actuator 探测缺少可验证环境证据。
- 本次实现结果：admin 模块骨架、API 契约、DDL/SQL 快照、持久化映射、RPC 登录、管理员 RBAC、鉴权拦截、审计、统一变更治理、治理适配器和第一版 REST API 已完成聚焦验证。
- 是否发布就绪：否。当前仍需完成 S2 风险门禁，并明确真实服务发现、auth RPC 和 Actuator 联调未验证风险。

## 2. 变更文件

- `spec/features/20260630/ddl-changes/INIT-sys_admin-admin-control-plane.sql`：替换为中文注释版 DDL，删除无明确场景的普通索引，并补充普通索引设计依据。
- `fons4cloud-auth/fons4cloud-auth-service/src/main/java/com/fons/cloud/auth/infrastructure/converter/OauthClientManagementConverter.java`：新增 OAuth Client 管理对象转换器。
- `fons4cloud-auth/fons4cloud-auth-service/src/main/java/com/fons/cloud/auth/facade/OauthClientManagementFacadeServiceImpl.java`：使用 Converter 替代重复字段搬运，补充密钥生成、脱敏和一次性返回规则注释。
- `fons4cloud-auth/fons4cloud-auth-service-api/src/main/java/com/fons/cloud/auth/**`：补齐 OAuth Client 管理 RPC 方法、Request、Response 字段注释。
- `fons4cloud-auth/fons4cloud-auth-service/src/test/java/com/fons/cloud/auth/facade/OauthClientManagementFacadeServiceImplTest.java`：新增契约级单元测试。
- `fons4cloud-admin/fons4cloud-admin-api/src/main/java/com/fons/cloud/admin/api/**`：补齐 admin Request/Response 关键字段注释。
- `.specify/sql/sys_admin/admin-control-plane.sql`：新增基于真实数据库结构的 admin 当前结构快照。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/domain/entity/**`：新增 10 张 admin 自有表的 MyBatis-Plus 实体。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/domain/mapper/**`：新增 10 个 admin Mapper，复用 `BasePlusMapper`。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/resources/mapper/**`：新增 Mapper XML 命名空间文件。
- `fons4cloud-admin/fons4cloud-admin-service/src/test/java/com/fons/cloud/admin/domain/AdminPersistenceMappingTest.java`：新增持久化映射结构契约测试。
- `fons4cloud-admin/fons4cloud-admin-api/src/main/java/com/fons/cloud/admin/api/request/AdminRefreshTokenRequest.java`：新增 admin 刷新 Token 请求。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/application/AdminAuthApplicationService.java`：新增 admin 认证应用服务。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/auth/**`：新增 admin 认证配置、RPC 客户端端口和 Dubbo 实现。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/infrastructure/converter/AdminAuthConverter.java`：新增 Token 响应转换器。
- `fons4cloud-admin/fons4cloud-admin-service/src/main/java/com/fons/cloud/admin/interfaces/rest/AdminAuthController.java`：新增 `/admin/auth` REST 入口。
- `fons4cloud-admin/fons4cloud-admin-service/src/test/java/com/fons/cloud/admin/application/AdminAuthApplicationServiceTest.java`：新增 admin 认证应用服务测试。
- `fons4cloud-admin/fons4cloud-admin-service/src/test/java/com/fons/cloud/admin/interfaces/rest/AdminAuthControllerTest.java`：新增 admin 认证 REST API 测试。

## 3. TDD 记录

| 任务 | RED | GREEN | REFACTOR |
| --- | --- | --- | --- |
| T001 | 纯模块骨架不适合先写业务测试，采用编译作为等价验证 | admin 相关模块编译通过 | 保持既有多模块 POM 组织 |
| T002 | API 契约任务采用编译和注释审查作为等价验证 | admin-api 编译通过 | 补齐 Token、状态、hash、外部资源引用等字段注释 |
| T003 | 新增 `OauthClientManagementFacadeServiceImplTest` 覆盖 OAuth Client 管理 RPC 安全边界 | 2 个单元测试通过 | 抽取 `OauthClientManagementConverter`，移除 facade 中重复字段设置；更新时空字段不覆盖原值 |
| T004 | DDL 草稿不适合先写业务测试，采用 DDL 审查清单作为等价验证 | DDL 覆盖 10 张目标表、主键、唯一约束、逻辑删除、乐观锁、时间字段和回滚草稿 | 全量中文注释；普通索引收敛为有查询场景的少量组合索引 |
| T005 | DDL 执行确认任务不适合写业务测试，采用只读数据库结构查询作为等价验证 | `sys_admin` schema、10 张目标表、字段数量、主键、唯一约束和索引均验证通过 | 明确记录验证实例为 MySQL 8.4.7 |
| T006 | SQL 当前结构快照任务不适合写业务测试，采用真实 `SHOW CREATE TABLE` 作为等价验证 | 已生成 `.specify/sql/sys_admin/admin-control-plane.sql` | 快照只记录当前结构，不包含连接、工具或查询过程 |
| T007 | 先以结构契约测试固定实体表名、公共字段继承、Mapper 泛型和 XML 命名空间 | `AdminPersistenceMappingTest` 4 个测试通过，admin-service 编译通过 | 状态/类型字段暂用 `String`，避免 API 枚举与 DDL 存储值未完全对齐时产生隐式持久化语义 |
| T008 | 新增应用服务和 REST API 聚焦测试，覆盖控制面客户端服务端填充、未绑定/禁用吊销、刷新和退出 RPC | `AdminAuthApplicationServiceTest` 5 个测试、`AdminAuthControllerTest` 3 个测试通过 | 抽取 `AdminAuthConverter` 和 `AdminAuthenticationClient`，避免应用服务直接依赖 Dubbo 注解和重复 Token 字段搬运 |

说明：网络依赖下载失败、JDK 版本问题和 PowerShell `-D` 参数解析问题均属于环境或命令问题，不记录为 RED。

## 4. 验证结果

- 单元测试命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-auth/fons4cloud-auth-service -am '-Dtest=OauthClientManagementFacadeServiceImplTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- 单元测试结果：通过，`Tests run: 2, Failures: 0, Errors: 0, Skipped: 0`。
- 编译验证命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-api,fons4cloud-admin/fons4cloud-admin-service,fons4cloud-auth/fons4cloud-auth-service -am '-DskipTests' compile`
- 编译验证结果：通过，admin-api、admin-service、auth-service 及其依赖模块均构建成功。
- T007 聚焦测试命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=AdminPersistenceMappingTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- T007 聚焦测试结果：通过，`Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`。
- T007 验证限制：本次未对真实 `sys_admin` 库执行插入、更新或逻辑删除，避免污染已执行环境；真实最小 CRUD 写库验证并入 T021 的隔离集成回归。
- T008 聚焦测试命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -am '-Dtest=AdminAuthApplicationServiceTest,AdminAuthControllerTest,AdminPersistenceMappingTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`
- T008 聚焦测试结果：通过，`Tests run: 12, Failures: 0, Errors: 0, Skipped: 0`。
- 验证注意事项：Maven 在受限沙箱中无法解析外部 BOM，已按规则提权后验证；这是环境限制，不是代码失败。

## 4.1 T005/T006 DDL 执行确认与快照结果

- 验证时间：2026-07-02
- 验证方式：使用 `fons4cloud-mysql` 只读查询 `information_schema` 和 `SHOW CREATE TABLE`。
- 当前连接库：未指定默认库，通过 `sys_admin` schema 查询。
- MySQL 版本：`8.4.7`。
- schema 查询结果：已发现 `sys_admin`。
- 目标表查询结果：已发现 `admin_user`、`admin_role`、`admin_permission`、`admin_user_role`、`admin_role_permission`、`admin_governance_resource`、`admin_governance_change`、`admin_governance_release`、`admin_governance_snapshot`、`admin_governance_audit`。
- 字段数量验证：10 张表字段数量分别为 14、10、10、9、9、13、16、16、13、18，与 DDL 草稿一致。
- 索引验证：主键、唯一约束和普通索引均已生效，包括 `uk_admin_user_account_id`、`uk_admin_role_code`、`uk_admin_permission_code`、`uk_admin_user_role`、`uk_admin_role_permission`、`uk_admin_gov_resource`、`uk_admin_gov_change_no`、`uk_admin_gov_release_no` 及治理查询索引。
- 判断：T005 验证通过，DDL 已在目标 MySQL 实例生效。
- 快照结果：已基于真实 `SHOW CREATE TABLE` 结果生成 `.specify/sql/sys_admin/admin-control-plane.sql`，T006 完成。

## 5. AC 覆盖

| AC | 覆盖任务 | 当前证据 |
| --- | --- | --- |
| AC-002 | T003 | OAuth Client 管理 RPC 仍由 auth-service 负责创建、修改、启停、轮换密钥和清缓存 |
| AC-004 | T003 | auth-service 单元测试覆盖创建加密、一次性密钥返回和缓存失效 |
| AC-005 | T004、T005、T006 | DDL 包含审计、快照、发布记录和回滚说明；已通过只读数据库查询确认 DDL 生效，并同步 SQL 当前结构快照 |
| AC-007 | T002、T003、T004、T007、T008 | admin 和 auth API 字段注释补齐，敏感字段排除 `toString`；admin 持久化模型不保存 `source_client_id`、密码和 token 明文；未绑定或禁用管理员登录时吊销 Token 并拒绝访问 |
| AC-008 | T001、T002、T003、T004、T007、T008 | Maven 编译验证通过，admin 模块、auth RPC 契约、admin 持久化映射和 `/admin/auth/**` REST 入口可编译并通过聚焦测试 |

## 6. 代码质量复盘

- 注释约束：已补齐 RPC 接口方法、Request、Response、密钥、Token、状态、hash、时间、外部资源引用等关键字段注释。
- Converter 约束：新增 `OauthClientManagementConverter`，替代 facade 中重复 Bean 属性设置。
- 安全边界：`clientSecret` 不进入普通响应视图；明文密钥只在创建或轮换结果中一次性返回，并通过 `@ToString.Exclude` 避免日志输出。
- DDL 约束：脚本说明、表注释、字段注释均改为中文；枚举值和代码标识保留英文但有中文语义说明。
- 索引约束：保留唯一约束和明确查询场景索引；删除无直接场景支撑的 hash、error_code、created_by、updated_by、授权时间等普通索引。
- TDD 约束：T003 已补聚焦单元测试；T001/T002/T004 属于骨架、契约和 DDL 草稿，使用编译或审查清单作为等价验证。
- 持久化约束：T007 实体复用 `CommonEntity` 的 `id/deleted/version/created/updated`，Mapper 复用 `BasePlusMapper`，避免重复定义公共字段。
- 枚举持久化约束：部分 API 枚举值与 DDL 存储值尚未完全一一对应，持久化实体先使用 `String` 保存状态和类型，后续由 application/domain 层显式转换。
- RPC 认证约束：T008 登录、刷新、退出均通过 `AccountAuthenticationFacadeService` Dubbo RPC；`clientSecret` 只来自 `admin.auth.client-secret` 服务端配置，不进入 Request/Response。
- 转换器约束：T008 使用 `AdminAuthConverter` 合并 `TokenInfo` 与 `AdminUser`，避免应用服务内重复 Bean 属性设置。

## 7. DDL 与数据结构状态

- 是否涉及 DDL：是。
- 执行型 DDL 草稿：已更新 `spec/features/20260630/ddl-changes/INIT-sys_admin-admin-control-plane.sql`。
- DDL 执行方式：用户或 DBA 手动执行。
- DDL 执行状态：已通过只读数据库查询确认执行。
- SQL 当前结构快照：已生成 `.specify/sql/sys_admin/admin-control-plane.sql`。
- 发布限制：T005/T006 已解除；T007 映射验证通过，但真实写库 CRUD 仍需在隔离集成环境补充；T008 已完成 RPC 认证入口但依赖 T009/T010 关闭完整授权链路。

## 8. 风险与后续事项

- 风险：T007 未对真实 `sys_admin` 执行写库测试，后续 T021 需要在隔离环境补齐最小 CRUD、乐观锁和逻辑删除验证。
- 风险：当前认证服务 `AccountInfo` 契约未暴露 `clientId`，T008 运行期只能保证使用服务端控制面客户端发起认证；“账号属于控制面客户端”的绑定准入校验需在 T009 通过认证服务账号查询能力或后续契约补充关闭。
- 风险：Maven 构建会生成 `target/` 和 `.flattened-pom.xml` 构建产物；本次未做清理，避免误删非源码内容。
- 后续事项：继续 T009，实现管理员绑定、禁用、角色授权、权限点初始化和首个 ROOT 初始化；真实写库验证在 T021 集成回归中关闭。
- 长期知识影响：是。涉及 admin 控制面模块、OAuth Client 管理 RPC、admin 自有库 DDL 和 SDD 实施规则约束；知识沉淀应由后续显式触发 `fons4ai-knowledge-summary` 处理。

## 9. 2026-07-09 CR-002 内置控制台实施记录

### 9.1 本次完成范围

- 完成 T027：`admin-service` 新增 Freemarker 页面承载能力，提供 `/admin-ui/**` 页面入口。
- 完成 T028：内置登录页支持调用 `/admin/auth/login`，前端脚本使用 `sessionStorage` 保存 Token，并为 `/admin/**` 请求注入 `Authorization: Bearer`。
- 完成 T029：补齐服务治理、网关治理、流量治理、授权资源、认证客户端、运行探测页面。
- 完成 T030：补齐变更中心和审计追踪页面，提供校验、发布、回滚和审计列表入口。
- 完成 T031：补齐页面路由、静态资源契约、Freemarker 渲染、模块全量测试和 Jar 资源检查证据。

### 9.2 关键实现说明

- 页面源码全部位于 `fons4cloud-admin/fons4cloud-admin-service` 内，不引入 Node/Vite/Vue 构建链。
- 页面 Controller 只返回页面壳和 `activeMenu/pageTitle` 导航模型，不查询服务列表、配置正文、审计详情、Token 或密钥。
- 真实数据统一由 `static/admin/js/admin-console.js` 调用现有 `/admin/**` API 获取。
- 前端菜单隐藏、按钮展示和页面跳转只作为用户体验控制，不作为安全边界；真实安全仍由 gateway `ADMIN` authority 和 admin-service 内部 RBAC 保证。
- 写操作包含二次确认和按钮禁用，避免误操作和重复提交。

### 9.3 验证结果

- 页面聚焦测试命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -Dtest=AdminPageControllerTest,AdminConsoleAssetContractTest,AdminFreemarkerTemplateRenderTest -Dsurefire.failIfNoSpecifiedTests=false test`
- 页面聚焦测试结果：通过，`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。
- admin-service 全量测试命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service test`
- admin-service 全量测试结果：通过，`Tests run: 89, Failures: 0, Errors: 0, Skipped: 0`。
- 打包命令：`$env:JAVA_HOME='C:\hongqy\C\Java\jdk21'; $env:Path="$env:JAVA_HOME\bin;$env:Path"; C:\hongqy\tool\apache-maven-3.9.8\bin\mvn.cmd -pl fons4cloud-admin/fons4cloud-admin-service -DskipTests package`
- 打包结果：通过，生成 `fons4cloud-admin/fons4cloud-admin-service/target/admin-service.jar`。
- Jar 资源检查：`BOOT-INF/classes/template/admin/**` 和 `BOOT-INF/classes/static/admin/**` 均已进入 Jar。

### 9.4 剩余风险

- 本次完成的是 `admin-service` 内置页面与等价前后端调用链验证，未直接执行真实 Nacos/Redis 写入、发布、漂移验证和清理。
- 真实 gateway Bearer 转发链路的既有风险仍以 S2 风险清单当前记录为准，不因本次内置页面完成而自动关闭 T022。
